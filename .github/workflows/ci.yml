name: CI/CD Pipeline

on:
  push:
    # THAY ĐỔI 1: Đổi thành "master" cho giống với repo của bạn
    branches: [ "master" ]

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
      image_name_lower: ${{ steps.set-names.outputs.lower }}

    steps:
      - name: Checkout code (Repo App)
        uses: actions/checkout@v4

      # THAY ĐỔI 2: Sửa lại comment cho rõ ràng
      # Bước này sẽ tự động lấy tên repo app (dinhvanan307/midterm_cicd)
      - name: Set lower-case image name
        id: set-names
        run: |
          # Biến này sẽ là 'dinhvanan307/midterm_cicd'
          LOWER_NAME=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=$LOWER_NAME" >> $GITHUB_ENV
          echo "lower=$LOWER_NAME" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          # Image sẽ là ghcr.io/dinhvanan307/midterm_cicd
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}
          tags: |
            type=sha,prefix=,format=short

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  update-manifest:
    name: Update Kubernetes Manifest
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Checkout code (Repo Manifest)
        uses: actions/checkout@v4
        with:
          # Dòng này đã đúng (trỏ đến repo manifest 'midterm-gitops')
          repository: dinhvanan307/midterm-gitops
          token: ${{ secrets.GIT_PAT }} 

      - name: Get new image tag and name
        run: |
          echo "NEW_TAG=${{ needs.build-and-push.outputs.image_tag }}" >> $GITHUB_ENV
          # Biến này sẽ là 'dinhvanan307/midterm_cicd'
          echo "IMAGE_NAME_LOWER=${{ needs.build-and-push.outputs.image_name_lower }}" >> $GITHUB_ENV

      - name: Update image tag in deployment.yaml
        run: |
          # Lệnh 'sed' này sẽ cập nhật image thành:
          # image: ghcr.io/dinhvanan307/midterm_cicd:new_tag
          sed -i "s|image: .*|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}:${{ env.NEW_TAG }}|g" deployment.yaml
          cat deployment.yaml

      - name: Commit and push changes
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "bot@github.com"
          git add deployment.yaml
          git commit -m "Update image to ${{ env.NEW_TAG }}" || echo "No changes to commit"
          git push